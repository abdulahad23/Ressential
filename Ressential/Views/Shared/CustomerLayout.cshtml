<!DOCTYPE html>
<html lang="en">
<head>
    <title>Ressenetial</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <link href="https://fonts.googleapis.com/css?family=Poppins:200,300,400,500,600,700,800&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lora:400,400i,700,700i&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Amatic+SC:400,700&display=swap" rel="stylesheet">


    <link rel="stylesheet" href="~/Content/CustomerAssets/css/open-iconic-bootstrap.min.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/css/animate.css">

    <link rel="stylesheet" href="~/Content/CustomerAssets/css/owl.carousel.min.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/css/owl.theme.default.min.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/css/magnific-popup.css">

    <link rel="stylesheet" href="~/Content/CustomerAssets/css/aos.css">

    <link rel="stylesheet" href="~/Content/CustomerAssets/css/ionicons.min.css">

    <link rel="stylesheet" href="~/Content/CustomerAssets/css/bootstrap-datepicker.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/CustomerAssets/css/jquery.timepicker.css">


    <link rel="stylesheet" href="~/Content/CustomerAssets/css/flaticon.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/css/icomoon.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/css/style.css">
    <link rel="stylesheet" href="~/Content/CustomerAssets/css/style2.css">
    <link rel="stylesheet" href="~/Content/assets/fonts/fontawesome.css">



</head>
<body class="goto-here">
    <div class="content-wrapper">
        <nav class="navbar navbar-expand-lg navbar-dark ftco_navbar bg-dark ftco-navbar-light" id="ftco-navbar">
            <div class="container">
                <a class="navbar-brand" href="@Url.Action("Index", "Customer")">Ressential</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#ftco-nav" aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="oi oi-menu"></span> Menu
                </button>

                <div class="collapse navbar-collapse" id="ftco-nav">
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Index" && ViewContext.RouteData.Values["controller"].ToString() == "Customer" ? "active" : "")">
                            <a href="@Url.Action("Index", "Customer")" class="nav-link">Home</a>
                        </li>
                        <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Shop" && ViewContext.RouteData.Values["controller"].ToString() == "Customer" ? "active" : "")">
                            <a href="@Url.Action("Shop", "Customer")" class="nav-link">Shop</a>
                        </li>
                        <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "About" && ViewContext.RouteData.Values["controller"].ToString() == "Customer" ? "active" : "")">
                            <a href="@Url.Action("About", "Customer")" class="nav-link">About</a>
                        </li>
                        <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "ContactUs" && ViewContext.RouteData.Values["controller"].ToString() == "Customer" ? "active" : "")">
                            <a href="@Url.Action("ContactUs", "Customer")" class="nav-link">Contact</a>
                        </li>
                        @if (Session["IsAuthenticated"] == null)
                        {
                            <!-- Login Button -->
                            <li class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Login" && ViewContext.RouteData.Values["controller"].ToString() == "Customer" ? "active" : "")">
                                <a href="@Url.Action("Login", "Customer")" class="nav-link">Login</a>
                            </li>
                        }
                        <li class="nav-item">
                            @if (Session["BranchId"] == null)
                            {
                                <a href="#" class="nav-link" id="selectBranchButton">Select Branch</a>
                            }
                            else
                            {
                                <a href="#" class="nav-link" id="selectBranchButton">@Session["BranchName"]</a>
                            }
                        </li>
                        <li class="nav-item cta cta-colored @(ViewContext.RouteData.Values["action"].ToString() == "Cart" && ViewContext.RouteData.Values["controller"].ToString() == "Customer" ? "active" : "")">
                            <a href="@Url.Action("Cart", "Customer")" class="nav-link"><span class="fa fa-shopping-cart"></span></a>
                        </li>
                        @if (Session != null && Session["IsAuthenticated"] != null && (bool)Session["IsAuthenticated"])
                        {
                            <!-- Profile Dropdown -->
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <span class="fa fa-user"></span> @Session["CustomerName"]
                                </a>
                                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                                    <a class="dropdown-item" href="@Url.Action("EditAccount", "Customer")">Edit Account</a>
                                    <a class="dropdown-item" href="@Url.Action("Logout", "Customer")">Logout</a>
                                </div>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
        <!-- END nav -->
        <div id="branchModal" class="modal">
            <div class="modal-content">
                <button id="closeBranchModal" class="close-button">&times;</button>
                <h2>Select Branch for Delivery</h2>
                <select id="branchDropdown" class="form-control">
                    <!-- Branch options will be loaded dynamically -->
                </select>
                <span>Selected Branch Id: @Session["BranchId"]</span>
                <span>@Session["BranchName"]</span>
                <button id="confirmBranchButton" class="btn btn-primary mt-3">Confirm Branch</button>
            </div>
        </div>

        @RenderBody();
        <footer class="ftco-footer ftco-section">
            <div class="container">
                <div class="row">
                    <div class="mouse">
                        <a href="#" class="mouse-icon">
                            <div class="mouse-wheel"><span class="ion-ios-arrow-up"></span></div>
                        </a>
                    </div>
                </div>
                <div class="row mb-5">
                    <div class="col-md">
                        <div class="ftco-footer-widget mb-4">
                            <h2 class="ftco-heading-2">Ressential</h2>
                            <p>Satisfy your cravings and indulge in our delicious creations!</p>
                            <ul class="ftco-footer-social list-unstyled float-md-left float-lft mt-5">
                                <li class="ftco-animate"><a href="https://web.whatsapp.com/" target="_blank"><span class="icon-whatsapp"></span></a></li>
                                <li class="ftco-animate"><a href="https://www.facebook.com/" target="_blank"><span class="icon-facebook"></span></a></li>
                                <li class="ftco-animate"><a href="https://www.instagram.com/" target="_blank"><span class="icon-instagram"></span></a></li>
                            </ul>
                        </div>
                    </div>
                    <div class="col-md">
                        <div class="ftco-footer-widget mb-4 ml-md-5">
                            <h2 class="ftco-heading-2">Menu</h2>
                            <ul class="list-unstyled">
                                <li><a href="@Url.Action("Shop", "Customer")" class="py-2 d-block">Shop</a></li>
                                <li><a href="#" class="py-2 d-block">About</a></li>
                                <li><a href="#" class="py-2 d-block">Journal</a></li>
                                <li><a href="#" class="py-2 d-block">Contact Us</a></li>
                            </ul>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="ftco-footer-widget mb-4">
                            <h2 class="ftco-heading-2">Help</h2>
                            <div class="d-flex">
                                <ul class="list-unstyled mr-l-5 pr-l-3 mr-4">
                                    <li><a href="#" class="py-2 d-block">Shipping Information</a></li>
                                    <li><a href="#" class="py-2 d-block">Returns &amp; Exchange</a></li>
                                    <li><a href="#" class="py-2 d-block">Terms &amp; Conditions</a></li>
                                    <li><a href="#" class="py-2 d-block">Privacy Policy</a></li>
                                </ul>
                                <ul class="list-unstyled">
                                    <li><a href="#" class="py-2 d-block">FAQs</a></li>
                                    <li><a href="#" class="py-2 d-block">Contact</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="col-md">
                        <div class="ftco-footer-widget mb-4">
                            <h2 class="ftco-heading-2">Have a Questions?</h2>
                            <div class="block-23 mb-3">
                                <ul>
                                    <li><span class="icon icon-map-marker"></span><span class="text">203 Fake St. Mountain View, San Francisco, California, USA</span></li>
                                    <li><a href="#"><span class="icon icon-phone"></span><span class="text">+2 392 3929 210</span></a></li>
                                    <li><a href="#"><span class="icon icon-envelope"></span><span class="text">info@yourdomain.com</span></a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12 text-center">

                    </div>
                </div>
            </div>
        </footer>
    </div>



    <!-- loader -->
    <div id="ftco-loader" class="show fullscreen"><svg class="circular" width="48px" height="48px"><circle class="path-bg" cx="24" cy="24" r="22" fill="none" stroke-width="4" stroke="#eeeeee" /><circle class="path" cx="24" cy="24" r="22" fill="none" stroke-width="4" stroke-miterlimit="10" stroke="#F96D00" /></svg></div>


    <script src="~/Content/CustomerAssets/js/jquery.min.js"></script>
    <script src="~/Content/CustomerAssets/js/jquery-migrate-3.0.1.min.js"></script>
    <script src="~/Content/CustomerAssets/js/popper.min.js"></script>
    <script src="~/Content/CustomerAssets/js/bootstrap.min.js"></script>
    <script src="~/Content/CustomerAssets/js/jquery.easing.1.3.js"></script>
    <script src="~/Content/CustomerAssets/js/jquery.waypoints.min.js"></script>
    <script src="~/Content/CustomerAssets/js/jquery.stellar.min.js"></script>
    <script src="~/Content/CustomerAssets/js/owl.carousel.min.js"></script>
    <script src="~/Content/CustomerAssets/js/jquery.magnific-popup.min.js"></script>
    <script src="~/Content/CustomerAssets/js/aos.js"></script>
    <script src="~/Content/CustomerAssets/js/jquery.animateNumber.min.js"></script>
    <script src="~/Content/CustomerAssets/js/bootstrap-datepicker.js"></script>
    <script src="~/Content/CustomerAssets/js/scrollax.min.js"></script>
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBVWaKrjvy3MaE7SQ74_uJiULgl1JY0H2s&sensor=false"></script>
    <script src="~/Content/CustomerAssets/js/google-map.js"></script>
    <script src="~/Content/CustomerAssets/js/main.js"></script>


    <style>
        /* Modal styles */
        .modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 9999;
        }

        .modal.show {
            display: flex;
        }


        .modal-content {
            position: relative;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            width: 400px;
            text-align: center;
        }

        .close-button {
            position: absolute;
            top: 10px;
            right: 10px;
            background: none;
            border: none;
            font-size: 20px;
            font-weight: bold;
            cursor: pointer;
            color: #000;
        }

        .close-button:hover {
            color: #ff0000;
        }

        body.modal-open {
            filter: blur(5px);
            overflow: hidden;
        }
    </style>

    <script>
    $(document).ready(function () {
        const modal = $('#branchModal');
        const dropdown = $('#branchDropdown');
        const confirmButton = $('#confirmBranchButton');
        const closeModalButton = $('#closeBranchModal');

         // Check if modal should be shown on page load
        @if (Session["BranchId"] == null)
        {
            <text>
            modal.addClass('show');
            populateBranches(); // Populate dropdown when modal is initially opened
            </text>
        }

        // Function to fetch branches and populate the dropdown
        function populateBranches(selectedBranchId = null) {
            $.getJSON('@Url.Action("GetBranches", "Customer")', function (data) {
                dropdown.empty();
                data.forEach(branch => {
                    const option = new Option(branch.BranchName, branch.BranchId);
                    if (branch.BranchId === selectedBranchId) {
                        option.selected = true; // Pre-select branch if it matches the current session branch
                    }
                    dropdown.append(option);
                });
            });
        }

        // Show modal and populate dropdown when the "Select Branch" button is clicked
        $('#selectBranchButton').click(function (e) {
            e.preventDefault();
            modal.addClass('show');
            populateBranches(@Session["BranchId"]); // Pass the current branch ID from the session
        });

        // Confirm branch selection
        confirmButton.click(function () {
            const selectedBranchId = dropdown.val();
            const selectedBranchName = dropdown.find('option:selected').text();

            // Send the selected branch to the backend to update the session
            $.post('@Url.Action("SetBranch", "Customer")',
                { BranchId: selectedBranchId, BranchName: selectedBranchName },
                function (response) {
                    if (response.success) {
                        // Update the branch name in the navigation bar
                        $('#selectBranchButton').text(selectedBranchName);
                        modal.removeClass('show');
                        location.reload();
                    } else {
                        alert('Failed to update branch. Please try again.');
                    }
                }
            ).fail(function () {
                alert('An error occurred while setting the branch.');
            });
        });

        // Close modal when the close button is clicked
        closeModalButton.click(function () {
            modal.removeClass('show');
        });

        // Close modal when clicking outside the modal content
        $(window).click(function (e) {
            if ($(e.target).is(modal)) {
                modal.removeClass('show');
            }
        });
    });
    </script>

</body>
</html>